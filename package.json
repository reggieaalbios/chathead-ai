{
  "name": "chathead-ai",
  "version": "0.1.0",
  "type": "module",
  "description": "AGS-based floating ChatHead AI overlay for Hyprland",
  "scripts": {
    "start": "ags -b chathead-ai -c ./config.js",
    "stop": "ags -b chathead-ai -q"
  }
}
